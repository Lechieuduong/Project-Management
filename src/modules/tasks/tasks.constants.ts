export enum TaskPriority {
    CRITICAL = 'Critical',
    LOW = 'Low',
    MEDIUM = 'Medium',
    HIGH = 'High',
    NONE = 'None'
}

export enum TaskMessage {
    TASK_EXIST = 'Task already exists'
}